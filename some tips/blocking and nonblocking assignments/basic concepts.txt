阻塞赋值(blocking assignments)"="：这种赋值方式是立即执行，阻塞赋值语句是按顺序执行的。

非阻塞赋值(non-blocking assignments)"<="：非阻塞赋值语句不会阻塞进程，直到整个块的操作执行完才一次性完成赋值操作。
程序运行到一条非阻塞语句时，将产生一个赋值事件，但并不立刻执行该赋值操作，而是继续往下执行，当整个块操作执行完之后，再一起执行所有的这些赋值事件。
